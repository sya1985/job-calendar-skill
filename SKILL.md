---
name: job-calendar
description: 给定「公司名 + 岗位 + 工作地点 + 社招/校招（默认社招）」，全网核查该公司在官网、招聘平台、资讯平台上的关联招聘信息，并生成可累积、可搜索的 HTML 报告。当用户说「查一下某公司某地的招聘」「帮我看看 XX 在 XX 有没有在招 XX 岗」「job search / 招聘核查」时使用。当用户输入「全量搜索」时，对报告中已勾选「加入全量搜索」的企业按各自最后一次搜索条件批量重新核查。
agent_created: true
---

# job-calendar 招聘信息核查技能

## 触发条件
- 用户给出「公司 + 岗位 + 工作地点」，希望核查该公司在各渠道是否有匹配的在招岗位，并要一份 HTML 报告（不输出 md）。
- 用户给出指令「全量搜索」（即 `@skill:job-calendar 全量搜索`），希望对报告中已勾选「加入全量搜索」的企业，按各自最后一次搜索条件批量重新核查（详见「全量搜索模式」一节；此模式下不需要、也不解析公司 / 岗位 / 工作地点）。

## ⚠️ 输入校验（强制，第一步执行，未通过不得进行任何搜索）
使用本技能时，**必须**同时满足以下三项，缺一不可：
- `company`（公司名）：公司全称，尽量连同别名/英文名一起检索。
- `position`（岗位）：岗位名称，越具体越好（如「测试开发」「后端 Go」）。
- `location`（工作地点）：城市/区（如「杭州」「深圳 南山」）。

**校验逻辑**：
0. **全量搜索模式优先**：若用户输入为「全量搜索」指令（见「全量搜索模式」一节），**直接跳过本校验与输入解析**，进入全量搜索流程；此时用户即使附带了公司 / 岗位 / 工作地点，也一律无视，不据此搜索。
1. 收到用户请求后，先逐项检查公司名、岗位、工作地点是否都已明确给出。
2. **若三项齐全** → 继续执行「输入解析」与「执行流程」（允许 `recruitType` 缺省，默认 `社招`）。
3. **若任一项缺失或含糊**（且非全量搜索模式）→ **立即停止，不得发起任何 WebSearch / WebFetch 等查询**，仅向用户返回错误提示，明确指出缺失的字段，请其补齐后再查。
   错误提示模板：
   > ❌ 缺少必填项，无法查询。请提供完整的「公司名 + 岗位 + 工作地点」：
   > - 公司名：{缺失则写「未提供」}
   > - 岗位：{缺失则写「未提供」}
   > - 工作地点：{缺失则写「未提供」}
   >
   > 示例：用 job search 查一下 拓竹科技 测试开发 杭州

注意：「社招/校招」不输入时默认按社招处理，**不属于必填项**，不得因此拦截查询。

## ⚠️ 同公司同日查询规则（强制，搜索前必做）
围绕「同一家公司 + 同一天」的查询，遵循 **「只保留最后一次搜索结果」** 原则，分两种情形：

**A. 完全相同条件（跳过，不重复搜索）**
判定维度为以下 5 项**全部一致**：公司（去括号归一化）、`queryDate`、`position`、`location`、`recruitType`（缺省社招）。
- 若已存在完全相同的记录 → **立即提示「✅ 该条件（{公司} {岗位} {地点} {社招/校招}）已于 {日期} 搜索过，无需重复搜索。」** 并**停止**，不发起任何 WebSearch / WebFetch，也不追加。

**B. 同公司同日但条件不同（重新搜索，覆盖旧结果）**
例如当天先搜「卡方 杭州」、后搜「卡方 上海」：
- 5 元组不完全一致 → **正常执行搜索**，但写入时 `build_report.py` 会**先删除当天该家所有旧记录、再追加本次最新结果**，最终只保留上海。
- 即：同一家公司同一天，无论改岗位 / 地点 / 社招校招，**只保留最后一次**查询的结果，之前的同天记录被丢弃。

> 二次兜底：`build_report.py` 在 `--entry` 追加时实现上述逻辑——同公司同日且 5 元组完全相同则打印 `[SKIP] 已搜索过：…` 并跳过；同公司同日但条件不同则移除当日旧 entry 后再追加，确保「只留最后一次」。

## 全量搜索模式（批量重新搜索，无需输入公司 / 岗位 / 地点）
当用户以「全量搜索」作为本次技能调用的**唯一指令**（即 `@skill:job-calendar 全量搜索`；其后若附带了公司 / 岗位 / 地点也一律无视）时，进入本模式。本模式的作用是：**对报告中已勾选「加入全量搜索」的企业，按它们各自「最后一次搜索的条件」重新核查一次**，免去逐家手输。

### 触发判定
- 用户输入去除空白后等于 `全量搜索`，或以 `全量搜索` 开头且其后没有有意义的条件（附带的公司名 / 岗位 / 地点视为无效、直接忽略）。
- 进入本模式后，**跳过「输入校验」与「输入解析」**：不要求 company / position / location，也不解析它们。

### 执行流程
1. **读取清单**：读取 `job_calendar_data.json`，筛选 `scheduled === true` 的全部 entry，按归一化公司名去重，得到「勾选了全量搜索」的公司集合。
   - 也可用本地服务接口 `GET /api/scheduled`（Node 服务）直接拿到去重后的公司列表及其「最后一次搜索条件」（公司完整名、position、location、recruitType、lastQueryDate）。
2. **空清单保护（强制）**：若没有任何公司 `scheduled === true` → **立即停止，仅向用户返回提示，不发起任何搜索**：
   > ℹ️ 当前没有公司勾选「全量搜索」。请先在报告里（或重新搜索某家公司时）勾选其「加入全量搜索」；之后在对话中输入「全量搜索」，即可按各公司最后一次搜索条件批量重新核查。
3. **逐家重新搜索（按各自最后一次搜索条件）**：对清单中每一家归一化公司：
   - 取该公司 `queryDate` 最大（最近）的一条 entry，作为「最后一次搜索条件」：`company`（用其**完整名**，含括号内别名 / 英文名）、`position`、`location`、`recruitType`（缺省社招）。
   - **一天内同条件只搜一次（强制，沿用普通搜索规则）**：判定维度 = 5 元组（归一化公司名、`queryDate = 今天`、`position`、`location`、`recruitType`）。若 `job_calendar_data.json` 中已存在**完全相同**的 5 元组 → **跳过该公司**，提示「✅ {公司} {岗位} {地点} {社招/校招} 已于今日（{今天}）搜索过，跳过。」，不发起任何 WebSearch / WebFetch。
   - 否则 → 按「执行流程」第 1~4 步正常做 WebSearch / WebFetch / 分类，但本次的 `company / position / location / recruitType` **强制使用上面取到的「最后一次搜索条件」**（不要使用用户输入 —— 本模式下用户输入被忽略）。
   - 生成 entry 时：`queryDate` 设为今天；`scheduled` 设为 `true`（保持该公司全量搜索勾选状态，便于日后继续批量搜索）；其余字段按正常流程填写。
   - 追加并渲染：`python scripts/build_report.py --entry entry_tmp.json --data job_calendar_data.json --html job_calendar_report.html`（或经本地服务重渲染）。`build_report.py` 的 5 元组去重会作为二次兜底——若今天已搜过相同条件会打印 `[SKIP]` 并不追加。
4. **汇总反馈**：结束后向用户汇报：
   - 本次实际（重新）搜索了几家、分别是什么；
   - 因「今日已搜过同条件」跳过了几家；
   - （可选）哪些没有新增在招信息。

> 说明：本模式沿用了普通搜索的全部规则（BOSS 反爬专项核查、新鲜度番茄、官方渠道汇总、数据局限性横幅、同公司同日「只留最后一次」等），仅把「搜谁 / 用什么条件」改为「清单里勾选全量搜索的公司 + 它们各自最后一次搜索条件」。


- `company`（必填）：公司全称，尽量连同别名/英文名一起检索（如「字节跳动」也搜「ByteDance」）。
- `position`（必填）：岗位名称，越具体越好（如「测试开发」「后端 Go」）。
- `location`（必填）：工作地点（城市/区，如「杭州」「深圳 南山」）。
- `recruitType`（选填，默认 `社招`）：`社招` 或 `校招`。

## 本地服务自动启动（推荐，默认开启）

为了让报告里的「加入全量搜索」勾选与 `×` 删除能真实落盘（持久化到 `job_calendar_data.json`），本技能在**每次执行前自动确保本地服务已在运行**，无需用户手动敲命令：

1. **探测**：用 Bash/PowerShell 检查 `127.0.0.1:8771` 是否已监听（如 `Get-NetTCPConnection -LocalPort 8771` 或 `curl -s http://127.0.0.1:8771/api/data` 返回 200）。
2. **若未运行 → 自动后台启动**：优先用 Node.js 托管运行时，命令：
   `node scripts/job_calendar_server.js --data job_calendar_data.json --html job_calendar_report.html --port 8771`
   通过 Bash 工具以 `run_in_background: true` 启动，记下任务 ID；启动后探测一次确认存活（返回 200 即 OK）。
3. **若已运行 → 直接复用**，不重复启动（避免端口冲突）。
4. **兜底（无 Node.js 环境）**：若环境确实没有 Node.js，退化为静态模式——删除改用 `python scripts/build_report.py --delete "公司名"`，「加入全量搜索」勾选不可用（提示用户启动服务）。WorkBuddy 自带托管 Node 运行时，通常无需兜底。

> 报告交互（勾选 / 删除）依赖此服务；不启动则双击 HTML 只能查看、删除不落盘。用户也可随时在对话里让 WorkBuddy 重启服务。`GET /api/data` 与 `GET /api/scheduled` 也由该服务提供。

## 执行流程
1. **规划搜索（并行 WebSearch）**，覆盖以下渠道，每组用 `query_keyword_groups` 多角度：
   - 官方：官网「加入我们 / 招聘」页、官方招聘平台（如飞书 `*.jobs.feishu.cn`）、官方公众号/微博。
   - 招聘平台：BOSS直聘、智联招聘、猎聘、前程无忧(51job)、拉勾。
   - 资讯/聚合：脉脉、牛客网、爱企查、企查查职位、职友集(JobUI)、百度资讯。
   - 关键词组合示例：`{company} {location} {position} 招聘`、`{company} {position} BOSS直聘 猎聘`、`{company} {location} {position} 校招/社招`。
2. **抓详情（WebFetch）**：对关键职位页抓取岗位名、地点、薪资、经验、学历、发布时间、JD、要求，**并务必核对页面是否已有「已截止 / 已关闭 / 已招满」等状态标识（见「数据准确性校验」第 2 条）**。注意 BOSS直聘/企查查详情页常需登录，无法打开时以搜索摘要为准并在 `warning` 标注。
> **⚠ BOSS 直聘反爬专项核查（强制）**：BOSS 职位页为 JS 动态渲染 + 反爬，WebFetch 抓到的是空壳、WebSearch 仅命中搜索引擎已收录的少量岗位，且结果按用户地理位置个性化，**存在严重漏检风险**。因此：
> - 每次必须针对目标岗位跑 **≥2 个 BOSS 定向搜索变体**（如 `{company} BOSS直聘 {position} {location}`、`{company} {position} {location} 在招`、`{company} 测试工程师 BOSS`），宁可多搜不可漏；
> - **严禁仅凭"搜索没返回"就下"无在招"结论**——必须显式在结论里说明"BOSS 实时在招情况以 App 内为准"；
> - 凡是**只从搜索摘要看到、无法用 WebFetch 逐项核验公司名与在招状态**的岗位，写入 `localPositions` 时设 `"verified": false`，渲染报告会打「⚠ 待 App 内确认」标记；已用官网/飞书/JobUI 等可核验来源确认的岗位不写该字段（默认视为已核验）。
3. **分类**：
  - `localPositions`：地点命中 `location` 的岗位。**⚠ 地点必须以来源页真实 base 为准（见「数据准确性校验」第 1 条），不得用查询的 `location` 套用；base 与查询地不符的岗位一律进 `otherCityPositions` 并注明真实 base。**（要点：岗位名、类型、来源+链接、地点、薪资、经验、学历、发布时间 `publishDate`、JD、要求、`warning`、**`verified`（可选布尔，仅当该岗位只是搜索摘要、未能逐项核验时写 `false`）**、已截止岗位写 `status: "已截止"` 且不放进本列表）
  - **新鲜度番茄（自动计算，无需手填，🍅 + 灰色🍅 双形态，共 5 个）**：脚本按 `publishDate` 与卡片 `queryDate` 的间隔自动渲染「新鲜度提示：」+ emoji，新鲜随时间递减、陈旧递增——≤7天 🍅×5 / ≤15天 🍅×4灰色🍅×1 / ≤30天 🍅×3灰色🍅×2 / ≤60天 🍅×2灰色🍅×3 / ≤90天 🍅×1灰色🍅×4 / >90天 灰色🍅×5。红色🍅=新鲜、灰色🍅（CSS `grayscale` 置灰）=陈旧。`warning` 字段**只写提示正文，不要带「新鲜度提示：」前缀**（前缀和 emoji 由模板统一生成）。
   - `otherCityPositions`：非关注地的岗位（弱化展示，仅列 岗位名/地点/来源/链接/备注）。
4. **整理** `channels`（仅汇总**官方招聘渠道**，如官网「加入我们」页、官方招聘平台飞书等；第三方平台不进此列表）与 `suggestions`（给用户的建议）。每条渠道必须带 `"official": true` 标记，渲染报告时只展示官方渠道。
5. **生成 entry JSON**（字段见下），写入临时文件，例如 `entry_tmp.json`。
6. **渲染 HTML**：运行脚本
   `python scripts/build_report.py --entry entry_tmp.json --data job_calendar_data.json --html job_calendar_report.html`
   （脚本会自动把新查询追加进 `job_calendar_data.json` 并重渲染同一份 `job_calendar_report.html`。）
   - **删除公司（UI 即时 + 服务端持久化，推荐用服务方式）**：
     - **启动本地服务**（推荐，删除可真实落盘；**仅需 Node.js、无需 Python 环境**）：`node scripts/job_calendar_server.js --data job_calendar_data.json --html job_calendar_report.html --port 8771`，浏览器打开 `http://127.0.0.1:8771/`。报告由服务实时渲染。每个公司 Tab 右侧（悬停出现）有 `×` 删除按钮 → `confirm` 确认 → 调用 `DELETE /api/company/<公司名>` 真实改写 `job_calendar_data.json` 并重渲染，视图同步刷新。服务方式的删除**立即持久化到本地数据文件**。服务同时提供 `GET /api/data`（读取全部数据）与 `GET /api/scheduled`（读取去重后的「已勾选全量搜索」公司列表及其最后一次搜索条件），供全量搜索模式使用。
     - **脚本永久删除（同步本地数据文件，Python CLI 备选）**：`python scripts/build_report.py --delete "公司名" --data job_calendar_data.json --html job_calendar_report.html`，按归一化公司名（相等或包含即匹配）移除该公司全部记录、改写本地 JSON 并重渲染（需 Python 环境）。
     - **直接双击 HTML 打开（无服务）**：删除按钮仍可点，但浏览器无法写本地文件，此时会退化为「仅从视图移除 + 下载更新后的 `job_calendar_data.json` 副本」，不会改本地数据文件；如需自动保存请先启动本地服务。
   - **报告交互**：左侧纵向 Tab 栏按公司分页（Tab 标签去括号内容，但企业页标题保留括号内别名/英文名）。点击某家公司后，该页展示：① 完整企业名（含括号内容）②「加入全量搜索」勾选框（状态经 `PUT /api/company/<名称>/scheduled` **持久化到 `job_calendar_data.json` 的 `scheduled` 字段**，不再用 localStorage；服务不可达时弹错提示；勾选后，在对话中输入「全量搜索」即可按各公司最后一次搜索条件批量重新核查，详见「全量搜索模式」）③ 企业简介 `intro` ④ 一个大日历控件（默认定位到最近一次查询月份；**有查询记录的日期高亮（蓝底），默认不选中任何日期**；某日若搜到本地确认岗位则该日显示单个 🍅（≤30天红 / >30天灰），否则显示 💔，发布日期无法确认显示 ❓；**点击有记录的日期 → 弹窗（modal）展示该日岗位卡片**，不在下方内联）⑤ 无顶部搜索框、无查询计数行。
7. **查看报告**：推荐启动本地服务后 `present_files` 打开 `http://127.0.0.1:8771/`（删除可持久化，启动命令见下「删除公司」）；也可直接 `present_files` 打开 `job_calendar_report.html`（仅查看，删除不落盘）。

## entry JSON 结构
```json
{
  "company": "拓竹科技（Bambu Lab）",
  "intro": "拓竹科技成立于 2020 年，总部深圳，专注桌面级 3D 打印机……（企业简介，用于企业页展示）",
  "position": "测试 / 测试开发",
  "location": "杭州",
  "recruitType": "社招",
  "localPositions": [
    {
      "title": "测试工程师（数据方向）",
      "type": "社招",
      "source": "企查查",
      "url": "https://...",
      "location": "杭州-西湖区",
      "salary": "30-60K·15薪",
      "experience": "3年以上",
      "education": "统招本科",
      "publishDate": "2026-03-18",
      "jd": "负责数仓/数据产品测试……",
      "requirements": "精通SQL……",
      "warning": "发布较早，建议投递确认",
      "verified": false
    }
  ],
  "_字段说明": "localPositions/otherCityPositions 中的岗位可带可选 `status` 字段（如 `\"已截止\"`）；一旦设置，报告会自动隐藏该岗位。岗位 `location` 必须填来源页真实 base，勿填查询地。",
  "otherCityPositions": [
    {"title":"软件测试工程师","location":"深圳","source":"飞书官方","note":"研发-测试","url":"https://..."}
  ],
  "channels": [
    {"name":"官网加入我们","url":"https://...","official":true},
    {"name":"飞书招聘主平台","url":"https://...","official":true}
  ],
  "suggestions": ["建议1","建议2"]
}

## 报告 HTML 结构（由脚本生成，无需手改）
- 顶部：仅标题，无搜索框、无查询计数。
- 左侧纵向 Tab：每家公司一项（标签去括号内容）。
- 企业页（点击 Tab 后）：完整企业名（含括号）+「加入全量搜索」勾选框（状态持久化到 `job_calendar_data.json`，非 localStorage）+ 企业简介 `intro` + 大日历控件（有查询记录日期蓝底高亮、默认不选中；日历图标用天气表情——☀️ 有本地岗且发布日期≤30天 / ⛅ 有本地岗且发布日期>30天 / ☁️ 未标注发布日期或无法确认新鲜度 / 🌧️ 该日未搜到本地岗位；图例同此；**点击日期弹窗展示当日卡片**）。
- 每张卡片：查询日期、查询条件徽章（地点/社招校招/岗位）、「本地确认岗位」（表格化展示，含「新鲜度提示：🍅灰色🍅灰色🍅」emoji 新鲜度体系，红🍅=新鲜/灰🍅=陈旧，提示正文加括号）、「非关注地岗位（供参考，弱化折叠）」、「官方招聘渠道」、「给你的建议」。
- 多次查询结果累积在同一 `job_calendar_report.html`，按公司 Tab 分页查看。
- 风格：浅色主题、中文、响应式。**查看**：本地双击 `job_calendar_report.html` 即可；**删除要持久化**请改用本地服务 `node scripts/job_calendar_server.js`（仅需 Node.js，浏览器打开 `http://127.0.0.1:8771/`），Tab 上悬停出现的 `×` 按钮会通过 `DELETE /api/company/<公司名>` 真实改写 `job_calendar_data.json`。

## ⚠️ 数据准确性校验（强制，写入 entry 前必做）

历史上出现过两类典型错误，根源都是「把用户给的查询条件，想当然地当成了来源页的真实字段」：
- **地点误判**：来源页实际 base 为「北京」的岗位，被误写进杭州 `localPositions`（用查询地套岗位地）。
- **状态漏检**：来源页已标「已截止」的岗位，仍当作在招写入 `localPositions`。
- 以及由此延伸的**类型对齐错误**：校招/实习岗被当作社招写入，导致报告按查询条件过滤时误隐藏/误展示。

写入任何 entry 前，必须逐条核对以下 4 条（对应「执行流程」第 2 步抓详情、第 3 步分类）：

1. **地点真实性校验（强制）**：岗位 `location` 必须以**来源页实际标注的 base / 工作地**为准，绝不能用查询的 `location` 直接套用。
   - 来源页明确写 base=某城市（如「Beijing」「深圳·南山」）→ 以真实 base 分类：与目标 `location` 一致才进 `localPositions`，否则进 `otherCityPositions`（并在 `note` 注明真实 base）。
   - 判断不了真实 base 时，宁可放 `otherCityPositions` 并标 `verified:false`，也不要擅自归入 `localPositions`。
2. **岗位状态核查（强制）**：抓职位页（WebFetch / 用户截图）时**必须检查页面是否有「已截止 / 已关闭 / 已暂停 / 该职位暂不招聘 / 已招满」等状态标识**。
   - 一旦发现 → 该岗位 `status` 写 `"已截止"`，**不要**放入 `localPositions` / `otherCityPositions` 的在招列表（报告按状态自动隐藏），并在 `warning` 或 `suggestions` 明确提示「该岗位已于来源页标注已截止」。
   - 绝不能把已截止岗位当作在招岗位写入。
3. **岗位类型对齐校验（强制）**：岗位 `type`（社招 / 校招 / 实习）必须来自来源页真实标注，不得用查询的 `recruitType` 套用。类型标错会导致报告按查询条件过滤时误隐藏 / 误展示。
4. **用户截图优先（强制）**：若用户提供了岗位页 / 列表页截图，以截图显示的**真实 base、岗位状态、类型**为最高依据，优先于搜索摘要。截图与摘要冲突时，以截图为准并在记录中注明「依据用户截图」。

> 记忆项：凡「想当然用查询条件代替来源页真实字段」都属于本类错误。每次写入前用上述 4 条逐一比对，可规避 90% 以上的数据失真。

## 注意事项
- 招聘平台反爬/登录限制：拿不到完整 JD 时如实标注，不臆造薪资或职责。
- **BOSS 直聘漏检风险（重点）**：BOSS 职位页动态渲染+反爬，WebFetch 抓空壳、WebSearch 仅命中已收录少量岗位且按地理位置个性化。本工具**无法保证搜全**，凡搜索摘要来源岗位必须标 `verified:false` 并由报告横幅提示用户去 App 内二次确认；结论里不得写「无在招」而不提此局限。
- 报告顶部自带「数据局限性说明」横幅，提醒用户 BOSS 岗位需 App 内确认。
- 时效性强：报告标注查询日期，投递前以平台实时页面为准。
- 默认输出 HTML，不生成 md。
